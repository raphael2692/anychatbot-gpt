import { NebularChatMessage } from './nebular-chat-message.model';

describe('NebularChatMessage', () => {
  it('should create an instance', () => {
    expect(new NebularChatMessage()).toBeTruthy();
  });
});
